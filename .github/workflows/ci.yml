name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  ci:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: azeel
          POSTGRES_PASSWORD: azeel_ci_password
          POSTGRES_DB: azeel_news
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U azeel -d azeel_news"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 5

    env:
      DATABASE_URL: postgresql://azeel:azeel_ci_password@localhost:5432/azeel_news
      JWT_SECRET: ci-only-secret-not-for-production
      NEXT_PUBLIC_SITE_URL: http://localhost:3000

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Prisma migrate (schema sync)
        run: npx prisma db push

      - name: Typecheck
        run: npm run typecheck

      - name: Lint
        run: npm run lint

      - name: Build
        run: npm run build
